<UserControl x:Class="WPFUI.PartialViews.Employees.EmployeesViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFUI.PartialViews.Employees"
             
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="8*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <DataGrid Margin="5" x:Name="EmployeesDataGrid" AutoGenerateColumns="False"
                  VerticalScrollBarVisibility="Auto"
        >
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ПІБ"           Width="*"    Binding="{Binding Path=Credentials}"  IsReadOnly="True"/>
                    <DataGridTextColumn Header="Допуски"       Width="0.5*" Binding="{Binding Path=AccessesWPF}"/>
                    <DataGridTemplateColumn Width="1*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <!--Click="EditAccesesBtn_OnClick"-->
                                <Button  VerticalAlignment="Center" HorizontalAlignment="Center">Редагувати допуски</Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Width="1*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <!--Click="DelBtn_OnClick"-->
                                <Button  VerticalAlignment="Center" HorizontalAlignment="Center">Видалити</Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
                <DataGrid.RowDetailsTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text=" Мінімальна кількість: " />
                            <TextBlock Text="{Binding Path=MinCount}" />
                        </StackPanel>
                    </DataTemplate>
                </DataGrid.RowDetailsTemplate>
            </DataGrid>
            <Grid Grid.Row="1">
                <Button Name="AddBtn" Style="{DynamicResource MaterialDesignRaisedAccentButton}" 
                    HorizontalAlignment="Center" VerticalAlignment="Center" Width="130" 
                    Background="AntiqueWhite" 
            >Додати товар</Button>
                <!--Click="AddBtn_OnClick"-->
            </Grid>
            <materialDesign:DialogHost Grid.Row="0" Grid.RowSpan="2" Name="GoodsDialogHost"/>
        </Grid>
    </Grid>
</UserControl>
